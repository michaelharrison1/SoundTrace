// This component is no longer used and has been replaced by RetroDNAVisualizer.tsx
// It can be safely deleted.
// Keeping an empty export to prevent build errors if it's still imported somewhere temporarily.
export {};
