// This component is no longer used as in-app playback functionality has been removed.
// Keeping the file ensures no build errors if it's imported elsewhere, but it renders nothing.
// Ideally, remove imports of this file from other components.

import React from 'react';

const SpotifyPlayerControls: React.FC = () => {
  return null;
};

export default SpotifyPlayerControls;
