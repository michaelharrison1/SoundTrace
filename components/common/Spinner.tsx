// This component is not used in the Windows 95 theme.
// Loading states are handled with simple text indicators.
// Keeping the file empty or removing it would be appropriate.
// For this exercise, I'll empty it to signify it's deprecated for this theme.

import React from 'react';

const Spinner: React.FC = () => {
  return null; 
};

export default Spinner;